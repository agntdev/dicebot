[
  {
    "name": "/count initialises DB, increments, and reads back the counter",
    "steps": [
      {
        "send": { "text": "/count" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Count: 1" } }
        ]
      },
      {
        "send": { "text": "/count" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Count: 2" } }
        ]
      },
      {
        "send": { "text": "/count" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Count: 3" } }
        ]
      }
    ]
  }
]