[
  {
    "name": "/help lists available commands",
    "steps": [
      {
        "send": { "text": "/help" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "Available commands:\n/start — Start the bot\n/help — Show available commands" } }
        ]
      }
    ]
  },
  {
    "name": "unknown command gets a friendly fallback",
    "steps": [
      {
        "send": { "text": "/blargh" },
        "expect": [
          { "method": "sendMessage", "payload": { "text": "I don't know that command. Try /help to see what I can do." } }
        ]
      }
    ]
  }
]
